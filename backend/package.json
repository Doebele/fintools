{
  "name": "portfolio-tracker-backend",
  "version": "2.0.0",
  "description": "Portfolio Tracker Backend — Synology NAS Edition",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon --watch server.js server.js",
    "health": "curl -s http://localhost:3001/api/health | python3 -m json.tool",
    "stats": "curl -s http://localhost:3001/api/stats  | python3 -m json.tool"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "better-sqlite3": "^9.4.3",
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "exceljs": "^4.4.0",
    "express": "^4.18.2",
    "express-rate-limit": "^7.2.0",
    "helmet": "^7.1.0",
    "multer": "^2.1.0",
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
